# Баримт / цахим гарын үсэг — ажлын төлөв

Хэрхэн ажилладгийг [`DOCUMENTS_SIGNING.md`](DOCUMENTS_SIGNING.md) хэлнэ. Энэ бичиг нь
**юу хийгдсэн, юу батлагдсан, юу хүлээгдэж байгааг** хэлнэ.

Хамгийн сүүлд шинэчилсэн: **2026-08-06**, `origin/main`-аас **47 commit урд**
(push хийгдээгүй — доорх «Хүлээгдэж байгаа» хэсгийг хар).

> **Rebase хийгдсэн.** `origin/main` 4 commit хөдөлсөн (#21…#24), тэдний гурав нь eID
> polling — яг миний ажилтай нийлдэг хэсэг. Rebase-ийн зөрчил нь зөвхөн текстийн
> (`StartSignature` ба `normalizeStart`-ын тайлбар хоёулаа үлдсэн), гэхдээ **утгын**
> зөрчил нэг байсан: upstream eID-ийн хугацааг зохиохоо больсон, миний гарын үсгийн
> зам зохиосоор байсан. Зассан — `DOCUMENTS_SIGNING.md` §1-ийг хар.

---

## 1. Дууссан

Blueprint-ийн **зургаан хуудас бүгд бодит** (mock үлдээгүй):

| Хуудас | Юу хийдэг |
|---|---|
| `/documents` | Баримтын жагсаалт, үүсгэх, батлахад илгээх, гарын үсэг, татгалзал, ул мөр |
| `/module/documents/approvals` | Батлах дараалал — хүлээж байгаа зүйл, хэн дараагийнх |
| `/module/documents/templates` | Загвар үүсгэх / засах / хүчингүй болгох / хэрэглэх |
| `/module/documents/workflows` | Төрөл тус бүрийн батлах хэлхээ (10 хүртэл шат) |
| `/module/documents/signatures` | Гарын үсгийн бодлого — суваг, «зөвхөн нэрлэсэн хүн» |
| `/module/documents/retention` | Хадгалах хугацаа ба хугацаа дууссаны тайлан |

Backend тал:

- **eID-ийн бодит батламжаар** гарын үсэг (`start` → `poll`), session нь баримттай
  холбогдсон, нэг удаагийн, хугацаатай.
- Баримт **өөрийн батлах хэлхээтэй** (`document_approval_steps`), шаардлагаа өөртөө
  агуулна (`required_signatures`) — төрлийн хэлхээ хожим засагдсан ч хүрэхгүй.
- Хэн зурж болохыг гурван зүйл шийднэ: `documents.sign` эрх, баримтын шат, бодлого.
- **Ledger** нь хоёр unique хязгаартай: нэг хүн нэг удаа, **нэг батламж нэг гарын
  үсэг** (`00017`).
- Аудит: `created`, `routed` (буусан хэлхээтэй), `signature_requested`, `signed`,
  `rejected` (тэр үеийн гарын үсгийн тоотой), тохиргоо, загвар.
- Migration `00010`…`00017`.
- Жагсаалт нь **хуудсаар** хариулна (200, дээд тал 500) бөгөөд **хэдийг харуулж
  байгааг хэлнэ**; «Дараагийнхыг ачаалах» нь `offset`-оор явдаг тул 500-аас хойшхи
  баримт ч хүрэгдэнэ. Батлах дараалал нь серверээс `PENDING_APPROVAL`-ыг **хуучнаас**
  эрэмбэлж авна. 20 мянган баримт: 5.9 MB → 60 KB.
- Жагсаалт дээр **хайлт** (гарчгаар, том/жижиг үсэг хамаарахгүй) ба **төрөл/төлөвийн
  шүүлт**. `%`, `_`, `!` гэх мэт тэмдэгтийг wildcard биш, бичсэн чигээр хайна
  (`ESCAPE '!'`).
- eID-ийн хугацааг **зохиодоггүй**: eID хэлсэн бол хадгална, хэлээгүй бол NULL,
  15 минутын backstop-той. Хүлээлтийг дуусгадаг нь eID-ийн `EXPIRED`.

---

## 2. Хэрхэн батлагдсан

- **46 тест** (unit + integration) бодит PostgreSQL 16 дээр. `go test -race ./...`
  ногоон, `golangci-lint` **0 issues**, `go vet` цэвэр.
- Frontend: `npx tsc --noEmit` ба `next build` цэвэр.
- Migration-ууд: `up → down → up` цэвэр DB дээр, бас **хуучин хэлбэртэй** мэдээн дээр:
  - давхардсан нэрлэсэн шат, 8 тэмдэгтээс багa дугаар, хэлбэржээгүй кирилл дугаар;
  - `1..n` биш дугаарлагдсан хэлхээ (2, 3 гэх мэт) ба байрлаагүй гарын үсэг;
  - нэг шат дээр хоёр/гурван гарын үсэг, бас хоёр өөр давхардал.
- **CI-ийн дарааллыг яг тэр эрэмбээр** нутагт гүйлгэсэн (vet → migrate → race тест →
  skip хамгаалалт → tsc → build): бүгд ногоон.
- HTTP smoke: бүтэн ceremony, загварын бүтэн мөчлөг, хязгаарууд (409/400/404/503),
  аудитын бүртгэлүүд.
- **Арван тойрог** олон агентын шүүлт (adversarial): олдсон ~60 бодит алдаа бүгд
  зассан. Тойрог бүр өмнөх тойргийн **засварыг** онилсон — олдсоны талаас нь илүү нь
  миний өөрийн шинэ засварын үр дагавар байсан.

---

## 3. Хүлээгдэж байгаа

### 3.1 Push (эзний оролцоо шаардана)

35 commit нутагт байна, `origin/main` рүү гараагүй. GitHub-д credential байхгүй:

```
fatal: could not read Username for 'https://github.com'
```

SSH key нь Mac дээр аль хэдийн байгаа — GitHub-д **нэмэх** л шаардлагатай
(https://github.com/settings/keys):

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJClVkH7M8Wi/veIH+7HA2BoJWJpT7cU7XLtQrvyIW1Q imseku-mac@e-bolovsrol-deploy
```

Нэмсний дараа:

```
git -C "~/gerege/open-gerege-mn-erp (gerege-systems)" push origin main
```

### 3.2 Ноорог төлөв — **шийдвэр хэрэгтэй**

`CreateDocument` баримтыг шууд `PENDING_APPROVAL` болгодог (`origin/main` дээр ч тийм
байсан), гэтэл `DRAFT` төлөв, «Батлахад илгээх» товч, `RouteDocument`, `documents.routed`
бүгд байгаа. Хоёр уншлага, хоёулаа ажиллаж байна — аль нь байх ёстойг эзэн шийднэ.
Дэлгэрэнгүйг `DOCUMENTS_SIGNING.md` §8-д бичсэн.

### 3.3 Платформын цоорхойнууд (документын §8)

- ДАН live клиент байхгүй (mock-only).
- Зурагчийг **нэвтэрсэн хэрэглэгчтэй** тулгадаггүй — `users`-д регистр байхгүй.
  (Аудит нь аль операторын session болохыг бичдэг.)
- Хадгалалт юу ч устгадаггүй — зориудаар, «хүн шийднэ».
- Эрхийн нэр зөвхөн англи (`PermissionDefinition`-д `Labels` байхгүй).

---

## 4. Дараа юу хийвэл зохих (тэргүүлэх дараалалаар)

1. **Push** (§3.1) — дараа нь CI-г хараад, 10.0.0.59 дээр deploy.
2. **Ноорог төлөвийн шийдвэр** (§3.2) — сонголт хийгдмэгц хэрэгжүүлэх нь жижиг ажил
   (`StatusPending` → `StatusDraft`, snapshot нь `RouteDocument` дээр аль хэдийн байгаа).
3. Хүсвэл: хүлээж байгаа баримтуудыг **шинэ хэлхээ рүү шилжүүлэх** үйлдэл (одоо
   зориудаар хүрдэггүй).
4. Хүсвэл: `users`-д регистр (эсвэл eID linking-аас гаргах) — тэгвэл зурагчийг
   оператортой тулгаж болно.
5. Хүсвэл: баримт тус бүрийн **өөрийн хуудас** (`/documents/[id]`) — одоо баримтыг
   зөвхөн жагсаалтын мөрөөр л ажиллуулна.
